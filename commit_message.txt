fix(ui): RoomJoin показывается только при готовом backend

Исправлена проблема с подключением к комнате при холодном старте backend на Render.

Изменения:
- Добавлена кнопка "🚪 Войти в комнату" с условным рендерингом
- RoomJoin управляется через showRoomJoin state
- Добавлена кнопка закрытия (×) в RoomJoin компонент
- Условие показа: connectionStatus.ai && connectionStatus.ws && !isConnected

Теперь правильная последовательность:
1. Разбудить backend (если спит)
2. Нажать "Войти в комнату"
3. Ввести данные и подключиться

Улучшен UX при работе с бесплатным tier Render.